-- ============================================================
-- 041: 工事管理 - 検査チェックリストの「開いたとき自動棚卸し」用カラム
-- 実行先: Supabase SQL Editor
-- 目的:
--   工事詳細を開いた際、前回から24時間以上経っていれば自動でAI棚卸しを実行する。
--   そのスロットル判定・二重実行防止のため、最終実行時刻を工事に保持する。
--   Render の Cron Job を別途立てなくても「実質1日1回」を実現する仕組み。
-- 冪等: 何度流しても安全。
-- ============================================================

ALTER TABLE construction_projects
  ADD COLUMN IF NOT EXISTS last_inspection_sweep_at TIMESTAMPTZ;
