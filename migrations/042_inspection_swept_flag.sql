-- ============================================================
-- 042: 工事管理 - 書類整理の「AI照合済み」記録カラム
-- 実行先: Supabase SQL Editor
-- 目的:
--   AI棚卸しを毎回「全書類」に対して行うのは無駄なので、
--   既にAIで照合した書類を記録し、未照合の書類だけを精査対象にする。
--   検査項目は固定（追加されず減るのみ）のため、一度照合した書類を
--   再照合しても新たな該当は生じない＝二度送らなくてよい。
-- 冪等: 何度流しても安全。
-- ============================================================

ALTER TABLE submission_documents
  ADD COLUMN IF NOT EXISTS inspection_swept_at TIMESTAMPTZ;
